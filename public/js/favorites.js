const favoritesButton = document.querySelector(".favoritesbtn");

favoritesButton.addEventListener("click", function(event) {
    event.preventDefault();

//     const favorites = await sequelize.query(
//         "INSERT INTO favorite INNER JOIN recipe ON favorite.recipe_id=recipe.id WHERE favorite.user_id=?", {
//             replacements: [req.session.user_id],
//             type: QueryTypes.SELECT,
//         }
//   );
//     const response = await fetch(`/profile`, {
//         method: 'POST',
//         body: {

//         }
//     })

    


 });